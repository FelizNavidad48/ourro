# tinyrepo

A tiny throwaway project used as a workspace for ourro QA tasks. It has a
two-function calculator (`calc.py`) and a plain-`python3` test file
(`test_calc.py`, run via `make test`). `calc.add` ships with a deliberate bug so
the "fix a failing test" task has something real to repair.
